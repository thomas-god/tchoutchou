[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.44", features = ["serde"] }
chrono-tz = "0.10"
derive_more = { version = "2.1.1", features = ["full"] }

[dev-dependencies]
pretty_assertions = "1.4.1"
